// Reserved for a future explicit diagnostics surface. Runtime state is kept
// close to its Session (status dot, context menu, and terminal overlay), so
// the app shell stays visually quiet.
export default function StatusBar() {
  return null;
}
